# Notebook 01 - Setup check

Questo file segna il primo notebook o punto di controllo del progetto.

Scopo:

- verificare che l'ambiente sia attivo
- verificare che `Archivio` sia montato
- verificare che i path di `.env` puntino a cartelle reali
- preparare il passo successivo: download e ispezione di `BioCube`

Prima di aprire un notebook vero:

```bash
source scripts/activate_project.sh
python scripts/check_project_setup.py
```
