# Copia questo file in `.env` e modifica i path in base al computer.
# Non committare mai `.env`, dataset, checkpoint o output.

BIOCUBE_DIR=/path/to/biocube
BIOANALYST_MODEL_DIR=/path/to/bioanalyst_pretrained
PROJECT_OUTPUT_DIR=/path/to/outputs
MPLCONFIGDIR=/path/to/local/matplotlib_cache
